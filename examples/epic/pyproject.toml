[build-system]
requires = ["setuptools>=45"]
build-backend = "setuptools.build_meta"

[project]
name = "noethers-turnstile-example-epic"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["noethers-turnstile", "scikit-learn>=1.3", "numpy>=1.24", "pandas>=2.0"]

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.setuptools.packages.find]
where = ["."]
include = ["adapter*", "acs*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
